# Implementation Plans

Generated by the `improve` skill on 2026-06-13, against commit `da89f42`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Standardize on a single lockfile and pin the package manager | P1 | S | — | DONE (pinned `pnpm@11.5.0` to match the local toolchain, not the example `9.x`) |
| 002  | Add a `typecheck` script | P1 | S | — | DONE |
| 003  | Add CI that runs lint, typecheck, registry test, and build | P1 | S | 001, 002 | DONE |
| 004  | Refactor `build-registry.mjs` to a data-driven loop | P2 | M | — | DONE (output byte-identical) |
| 005  | Single source of truth for component registration | P2 | M | 004 | DONE (manifest in `registry.components.json`; coverage checks added) |
| 006  | Defer-mount WebGL buttons on the landing grid | P3 | M | — | DONE (Step 0 runtime profiling not run; verified by build/typecheck/lint) |
| 007  | Fix or remove the misleading `highlightCode` stub | P3 | S | — | DONE (Branch B — `CodeBlock`/`highlightCode` were dead, removed) |
| 008  | Design: a single `add-component` generator (spike) | P3 | M | 005 | DONE (design notes + manifest-append prototype; full round-trip deferred to phases 2/3) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Dependency notes

- **003** wants **001** (so CI installs with the one chosen package manager) and
  **002** (so CI can call `pnpm typecheck`).
- **005** requires **004**: the data-driven manifest introduced for the registry
  builder is the same manifest 005 extends to the MDX and landing registrations.
- **008** is a design/spike that assumes the manifest from **005** exists; it
  specs the generator that writes into that manifest.
- **004 → 005 → 008** form the "component registration" track. 001/002/003 are
  independent quick wins (the verification baseline) and should land first.

## Findings considered and rejected

- **`<img>` instead of `next/image` on the landing logo** (`components/landing/landing-page.tsx:229`):
  single small static asset, negligible LCP cost. Not worth a plan.
- **Public `/test/registry` page shipped to production** (`app/test/registry/page.tsx`):
  exposes only already-public registry data; harmless. Optionally gate behind a
  non-prod check later, but not worth a dedicated plan.
- **`CommandButton` re-subscribes its `keydown` listener on every render when
  `onCommand` is an inline prop** (`components/evil-buttons/command-button.tsx:77-88`):
  one global listener, cheap to re-bind; cosmetic. Not worth a plan.
- **Mixed default vs named exports across button components**: stylistic only;
  both import styles already work everywhere. Not worth a plan on its own —
  plan 005 documents the split rather than churning every file.
- **Security**: no actionable findings. Static, auth-less, DB-less docs site;
  the `/raw/[...slug]` and markdown readers resolve through fumadocs'
  validated `source.getPage()` (no path traversal), and there are no secrets in
  the tree.
